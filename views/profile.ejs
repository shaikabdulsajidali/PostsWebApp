<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>  
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>
<body class="h-100% bg-zinc-900 ">
    <div class="w-full h-full bg-zinc-900 text-white p-5 px-10 ">
        <div class="flex justify-between items-center bg-zinc-700 mb-5 rounded-md">
            <h3 class="text-3xl font-bold m-8 text-center w-full pl-22">Profile page</h3>
            <a href="/logout" class="bg-red-500 px-7 py-2 font-bold rounded-lg mr-7  ">Logout</a>
        </div>
        
        <h3 class="text-3xl font-bold ">Hello, <%= user.name %>👋 </h3> 
        <h5 class="mb-5">you can create a post now</h5> 
        <div class="border-2 px-5 py-8  w-1/3 m-auto text-center rounded-lg bg-zinc-800  outline-none border-[1px] border-zinc-600">
            <form action="/post" method="post">
                <h3 class="text-2xl mb-9 font-bold">Create new post</h3>
                <textarea class="border-zinc-700 w-full p-3 block outline-none border-2 h-40 resize-none rounded-md mb-2 bg-zinc-700 " placeholder="What's on your mind ?" name="content"></textarea>
                <input type="submit" class="bg-blue-500 px-15 py-2 rounded-lg mt-5"  value="create">
            </form>
        </div>
        

        <div class="posts mt-20 ">
            <h4 class="text-zinc-500 mb-2">your posts</h4>
            <div class="postcontainer flex gap-1 flex-wrap">
                <% user.posts.reverse().forEach((post)=>{ %>  <!-- to get in reverse order  -->
                    <div class="eachpost p-3 border-2 border-zinc-700 w-110  rounded-md  ">
                        <h4 class="text-blue-400"><%= user.username %></h4>
                        <p><%= post.content %></p>
                        <div class="btns mt-5 flex justify-between ">
                            <a href="/like/<%= post._id %>" class="text-blue-400 text-md">
                                <small class="text-sm"><%= post.likes.length %></small> 
                                <%= post.likes.indexOf(user._id) === -1? "Like" :"Unlike" %>
                                <i class="fa-regular fa-heart"></i>
                            </a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-500 pr-2">edit </a>
                        </div>
                    </div>
                <% }) %>
         
            </div>
            
        </div>
        
    </div>
</body>
</html>